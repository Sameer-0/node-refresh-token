<%- include("../partials/head") %>
<%- include("../partials/leftSidebar") %>
<%- include("../partials/header") %>
<!--Notificaion icon-->
<!--HEADER END-->

<div class="main-content">
    <!--DASHBOARD CONTENT START-->
    <div class="breadcrumbs-container">
    
        <ul class="breadcrumb">
            <% if(breadcrumbs){%> <% for (let crumbs of breadcrumbs){%>
                <li><a href="<%- crumbs.url%>"><%- crumbs.name%></a></li>
                <%} }%>
        </ul>
    </div>
    

    <div class="row my-3 gx-3 gy-3 row1-data">
        <div class="col-xl-2 col-lg-4 col-md-6 ">
            <a href="/admin/schooltime/types">
                <div class="card row1-data-card text-center">
                    <div class="card-body d-flex flex-column align-items-center justify-content-center">
                        <img src="/icon_imgs/room-type.PNG" alt="">
                        <p class="fw-normal pt-1">School Timing TypeS</p>
                    </div>
                </div>
            </a>
        </div>
    </div>



    <div class="card table-card">
        <div class="card-header table-card-header text-uppercase d-flex align-items-center justify-content-between">
            <div>
                <h5>SCHOOL TIMING LIST</h5>
            </div>
            <div>
                <button class="btn add-btn ms-auto" data-bs-toggle="modal" data-bs-target="#add-schooltiming-modal"><i
                        class="fas fa-plus"></i></button>
                <!-- <button class="btn ms-auto delete-campuses add-btn "><i class="fas fa-trash"></i></button>
                <button class="btn ms-auto bulk-delete-campuses add-btn "><img src="/icons/delete.png" alt=""
                        width="26px" height="32px"></button> -->
            </div>
        </div>

        <div class="card-body table-responsive">
            <div class="table-searchbar-container"><button type="button"><i class="fas fa-search"></i> </button><input
                    type="search" id="searchkeyword" placeholder="Enter keywords" class="table-searchbar"></div>
            <div id="errorHtml"></div>
            <table class="table table-bordered custom_row" id="divisionTable">
                <thead>
                    <th>#</th>
                    <th>Name</th>
                    <th>Program Name</th>
                    <th>Program Name</th>
                    <th>Session</th>
                    <th>Start Time</th>
                    <th>End Time</th>
                    <th>School Timing Type</th>
                    <th>Day</th>
                    <th>Action</th>
                </thead>
                <tbody>
                    <%let count = 1;%>
                    <% for(let schooltime of schoolTimingList) {%>
                    <tr>
                    <td>#</td>
                    <td><%-schooltime.name%></td>
                    <td><%-schooltime.program_name%></td>
                    <td><%-schooltime.program_id%></td>
                    <td><%-schooltime.acad_session%></td>
                    <td><%-schooltime.start_time%></td>
                    <td><%-schooltime.end_time%></td>
                    <td><%-schooltime.schooltimetype%></td>
                    <td><%-schooltime.day_name%></td>
                     <td>
                        <i class="fas fa-edit text-dark cursor-pointer edit-school-time-type"
                            data-time-type-id="<%- schooltime.id %>"></i>
                            <i class="fas fa-trash text-dark cursor-pointer edit-school-time-type"
                            data-time-type-id="<%- schooltime.id %>"></i>
                    </td>
                    </tr>
                    <%}%>
                </tbody>
            </table>
            <div class="d-flex justify-content-between">
                <div>
                    <p>Total entries:<%- pageCount %></p>
                </div>
                <div>
                    <p id="pagination"></p>
                </div>
            </div>
</div>
</div>
</div>


<!-- MODAL FOR SCHOOL TIMING -->
<div class="modal fade" id="add-schooltiming-modal" tabindex="-1" role="dialog" aria-labelledby="add-holiday-modal" aria-hidden="true">
    <div class="modal-dialog modal-fullscreen" role="document">
        <div class="modal-content modal-inputform">
            <div class="modal-header  modal-inputform-header">
                <h5 class="modal-title text-dark me-2" id="add-org-modal-title">Add School Timing</h5>
                <button type="button" class="btn add-btn fs-6 text-dark me-auto" id="add-more-schoolTiming"><i class="fa-solid fa-plus"></i>
                </button>
                <button type="button" class="btn fs-4 text-dark" data-bs-dismiss="modal" aria-label="Close"><i class="fas fa-times"></i>
                </button>
                
            </div>
            <div class="modal-body">
                <div id="errorHtml"></div>
                <div class="d-flex justify-content-around mt-5">
                    <div class="form-check flex-fill d-flex justify-content-center">
                        <input class="form-check-input schoolTimingOptions me-1" type="radio" name="flexRadioDefault" id="sameForAllDay" checked>
                        <label class="form-check-label" for="sameForAllDay">
                        Same For All Day
                        </label>
                    </div>
                    <div class="form-check flex-fill d-flex justify-content-center">
                        <input class="form-check-input schoolTimingOptions me-1" type="radio" name="flexRadioDefault" id="DayWise">
                        <label class="form-check-label" for="DayWise">
                        Day Wise
                        </label>
                    </div>
                    <div class="form-check flex-fill d-flex justify-content-center">
                        <input class="form-check-input schoolTimingOptions me-1" type="radio" name="flexRadioDefault" id="programAndDayWise" >
                        <label class="form-check-label" for="programAndDayWise">
                        Program And Day Wise
                        </label>
                    </div>
                    <div class="form-check flex-fill d-flex justify-content-center">
                        <input class="form-check-input schoolTimingOptions me-1" type="radio" name="flexRadioDefault" id="programDaySessionWise" >
                        <label class="form-check-label" for="programDaySessionWise">
                        Program, Day And Session Wise
                        </label>
                    </div>
                </div>
                <div class="modal-body schoolTimingOptionsContainer">
               
                    <div class="table-responsive sameForAllDay" data-dis="true">
                        <table class="table table-bordered" id="add-more-sameForAllDay-table">
                            <thead>
                                <th>Start Time</th>
                                <th>End Time</th>
                                <th>School Timing Type</th>
                                <th>Action</th>
                            </thead>
                            <tbody>                            
                                <tr>
                                    <td>
                                        <select class="select2 modalSelect2" name="startTime">
                                            <% for(time of slotTime){%>
                                                <option value="<%- time.id%>"><%- time.start_time %></option>
                                            <%}%>
                                        </select>
                                    </td>
                                    <td>
                                        <select class="select2 modalSelect2" name="endTime">
                                            <% for(time of slotTime){%>
                                                <option value="<%- time.id%>"><%- time.end_time %></option>
                                            <%}%>
                                        </select>
                                    </td>
                                    <td>
                                        <select name="schoolTimingType" id="" class="select2 modalSelect2" >
                                            <% for(type of schoolTimingTypeList){%>
                                                <option value="<%- type.id%>"><%- type.name%></option>
                                            <%}%>
                                        </select>
                                    </td>
                                    <td>
                                        <i class="far fa-edit d-none"></i>
                                        <i class="fas fa-trash-alt text-danger cursor-pointer remove-schoolTiming"></i>
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
    
                   
                </div>
            
        </div>
        <div class="modal-footer">
            <button type="button" class="btn createSchoolTiming modal-inputform-btn">CREATE</button>
        </div>
    </div>
</div>

<%- include("../partials/footer") %>


<script>
    $(document).ready(function () {


          //Options for school timing
          $('.schoolTimingOptions').on('input', function myFunction(){
            console.log($(this).attr('id'))

            if($(this).attr('id') == 'DayWise'){
                let DayWise = `<div class="table-responsive DayWise" data-dis="true">
                    <table class="table table-bordered" id="add-more-DayWise-table">
                        <thead>
                            <th>Day</th>
                            <th>Start Time</th>
                            <th>End Time</th>
                            <th>School Timing Type</th>
                            <th>Action</th>
                        </thead>
                        <tbody>                           
                            <tr>
                                <td>
                                    <select class="select2 modalSelect2" name="day">
                                        <% for(day of dayList){%>
                                            <option value="<%- day.id%>"><%- day.day_name%></option>
                                        <%}%>
                                    </select>
                                </td>
                                <td>
                                    <select class="select2 modalSelect2" name="startTime">
                                        <% for(time of slotTime){%>
                                            <option value="<%- time.id%>"><%- time.start_time %></option>
                                        <%}%>
                                    </select>
                                </td>
                                <td>
                                    <select class="select2 modalSelect2" name="endTime">
                                        <% for(time of slotTime){%>
                                            <option value="<%- time.id%>"><%- time.end_time %></option>
                                        <%}%>
                                    </select>
                                </td>
                                <td>
                                    <select name="schoolTimingType" id="" class="select2 modalSelect2" >
                                        <% for(type of schoolTimingTypeList){%>
                                            <option value="<%- type.id%>"><%- type.name%></option>
                                        <%}%>
                                    </select>
                                </td>
                                <td>
                                    <i class="far fa-edit d-none"></i>
                                    <i class="fas fa-trash-alt text-danger cursor-pointer remove-schoolTimingDayWise"></i>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>`
                $('.schoolTimingOptionsContainer').html(DayWise);
                $('.select2').select2({
                dropdownParent: $('#add-schooltiming-modal')});
            }
            else if($(this).attr('id') == 'programAndDayWise'){
                let programAndDayWise = `<div class="table-responsive programAndDayWise" data-dis="true">
                    <table class="table table-bordered" id="add-more-programAndDayWise-table">
                        <thead>
                            <th>Program Name</th>
                            <th>Day</th>
                            <th>Start Time</th>
                            <th>End Time</th>
                            <th>School Timing Type</th>
                            <th>Action</th>
                        </thead>
                        <tbody>                           
                            <tr>
                                <td>
                                    <select class="select2 modalSelect2" name="programName">
                                        <% for(program of programList){%>
                                            <option value="<%- program.id%>"><%- program.program_name%></option>
                                        <%}%>
                                    </select>
                                </td>
                                <td>
                                    <select class="select2 modalSelect2" name="day">
                                        <% for(day of dayList){%>
                                            <option value="<%- day.id%>"><%- day.day_name%></option>
                                        <%}%>
                                    </select>
                                </td>
                                <td>
                                    <select class="select2 modalSelect2" name="startTime">
                                        <% for(time of slotTime){%>
                                            <option value="<%- time.id%>"><%- time.start_time %></option>
                                        <%}%>
                                    </select>
                                </td>
                                <td>
                                    <select class="select2 modalSelect2" name="endTime">
                                        <% for(time of slotTime){%>
                                            <option value="<%- time.id%>"><%- time.end_time %></option>
                                        <%}%>
                                    </select>
                                </td>
                                <td>
                                    <select name="" id="" class="select2 modalSelect2" >
                                        <% for(type of schoolTimingTypeList){%>
                                            <option value="<%- type.id%>"><%- type.name%></option>
                                        <%}%>
                                    </select>
                                </td>
                                <td>
                                    <i class="far fa-edit d-none"></i>
                                    <i class="fas fa-trash-alt text-danger cursor-pointer remove-schoolTimingProgramWise"></i>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>`
                $('.schoolTimingOptionsContainer').html(programAndDayWise);
                $('.select2').select2({
                dropdownParent: $('#add-schooltiming-modal')});

            }
            else if($(this).attr('id') == 'programDaySessionWise'){
                let programAndDayWise = `<div class="table-responsive programDaySessionWise" data-dis="true">
                    <table class="table table-bordered" id="add-more-programDaySessionWise-table">
                        <thead>
                            <th>Program Name</th>
                            <th>Acad Session</th>
                            <th>Day</th>
                            <th>Start Time</th>
                            <th>End Time</th>
                            <th>School Timing Type</th>
                            <th>Action</th>
                        </thead>
                        <tbody>                           
                            <tr>
                                <td>
                                    <select class="select2 modalSelect2 programNameForSession" name="programName" id="">
                                        <% for(program of programList){%>
                                            <option value="<%- program.id%>"><%- program.program_name%></option>
                                        <%}%>
                                    </select>
                                </td>
                                <td>
                                    <select class="select2 modalSelect2 sessionForProgram" name="acadSession" id="">
                                        <% for(acadSession of AcadSessionList){%>
                                            <option value="<%- acadSession.id%>"><%- acadSession.acad_session %></option>
                                        <%}%>
                                    </select>
                                </td>
                                <td>
                                    <select class="select2 modalSelect2" name="day">
                                        <% for(day of dayList){%>
                                            <option value="<%- day.id%>"><%- day.day_name%></option>
                                        <%}%>
                                    </select>
                                </td>
                                <td>
                                    <select class="select2 modalSelect2" name="startTime">
                                        <% for(time of slotTime){%>
                                            <option value="<%- time.id%>"><%- time.start_time %></option>
                                        <%}%>
                                    </select>
                                </td>
                                <td>
                                    <select class="select2 modalSelect2" name="endTime">
                                        <% for(time of slotTime){%>
                                            <option value="<%- time.id%>"><%- time.end_time %></option>
                                        <%}%>
                                    </select>
                                </td>
                                <td>
                                    <select name="schoolTimingType" id="" class="select2 modalSelect2" >
                                        <% for(type of schoolTimingTypeList){%>
                                            <option value="<%- type.id%>"><%- type.name%></option>
                                        <%}%>
                                    </select>
                                </td>
                                <td>
                                    <i class="far fa-edit d-none"></i>
                                    <i class="fas fa-trash-alt text-danger cursor-pointer remove-schoolTimingProgramSessionWise"></i>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>`
                $('.schoolTimingOptionsContainer').html(programAndDayWise);
                $('.select2').select2({
                dropdownParent: $('#add-schooltiming-modal')});
            }
            else{
                let dayOnly = `   <div class="table-responsive sameForAllDay">
                    <table class="table table-bordered" id="add-more-sameForAllDay-table">
                        <thead>
                            <th>Start Time</th>
                            <th>End Time</th>
                            <th>School Timing Type</th>
                            <th>Action</th>
                        </thead>
                        <tbody>                           
                            <tr>
                                <td>
                                    <select class="select2 modalSelect2" name="startTime">
                                        <% for(time of slotTime){%>
                                            <option value="<%- time.id%>"><%- time.start_time %></option>
                                        <%}%>
                                    </select>
                                </td>
                                <td>
                                    <select class="select2 modalSelect2" name="endTime">
                                        <% for(time of slotTime){%>
                                            <option value="<%- time.id%>"><%- time.end_time %></option>
                                        <%}%>
                                    </select>
                                </td>
                                <td>
                                    <select name="schoolTimingType" id="" class="select2 modalSelect2" >
                                        <% for(type of schoolTimingTypeList){%>
                                            <option value="<%- type.id%>"><%- type.name%></option>
                                        <%}%>
                                    </select>
                                </td>
                                <td>
                                    <i class="far fa-edit d-none"></i>
                                    <i class="fas fa-trash-alt text-danger cursor-pointer remove-schoolTiming"></i>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>`
                $('.schoolTimingOptionsContainer').html(dayOnly);
                $('.select2').select2({
                dropdownParent: $('#add-schooltiming-modal')});
            }
        })

        $('.select2').select2({
        dropdownParent: $('#add-schooltiming-modal')});

        //Add more rows for School timing
        $('#add-more-schoolTiming').on('click', function(){
          console.log($('.sameForAllDay[data-dis]').attr('data-dis'));
            if($('.sameForAllDay[data-dis]').attr('data-dis') == 'true'){
                console.log('hello')
                let lastTr = $('#add-more-sameForAllDay-table tbody tr:last-child')
                lastTr.find('.modalSelect2').select2('destroy');
                let startTime = lastTr.find(`select[name="startTime"]`).val();
                let endTime = lastTr.find(`select[name="endTime"]`).val();
                let schoolTimingType = lastTr.find(`select[name="schoolTimingType"]`).val();
                let clonedTr = lastTr.clone();

                lastTr.find('.modalSelect2').select2({
                    dropdownParent: $('#add-schooltiming-modal')
                });

                clonedTr.find('.modalSelect2').select2({
                    dropdownParent: $('#add-schooltiming-modal')
                });

                $('#add-more-sameForAllDay-table tbody').append(clonedTr)
             }
            else if($('.DayWise[data-dis]').attr('data-dis') == 'true'){
                let lastTr = $('#add-more-DayWise-table tbody tr:last-child')
                lastTr.find('.modalSelect2').select2('destroy');
                let day = lastTr.find(`select[name="day"]`).val();
                let startTime = lastTr.find(`select[name="startTime"]`).val();
                let endTime = lastTr.find(`select[name="endTime"]`).val();
                let schoolTimingType = lastTr.find(`select[name="schoolTimingType"]`).val();
                let clonedTr = lastTr.clone();

                lastTr.find('.modalSelect2').select2({
                    dropdownParent: $('#add-schooltiming-modal')
                });

                clonedTr.find('.modalSelect2').select2({
                    dropdownParent: $('#add-schooltiming-modal')
                });

                $('#add-more-DayWise-table tbody').append(clonedTr)
            }
            else if($('.programAndDayWise[data-dis]').attr('data-dis') == 'true'){
                let lastTr = $('#add-more-programAndDayWise-table tbody tr:last-child')
                lastTr.find('.modalSelect2').select2('destroy');
                let programName = lastTr.find(`select[name="programName"]`).val();
                let day = lastTr.find(`select[name="day"]`).val();
                let startTime = lastTr.find(`select[name="startTime"]`).val();
                let endTime = lastTr.find(`select[name="endTime"]`).val();
                let schoolTimingType = lastTr.find(`select[name="schoolTimingType"]`).val();
                let clonedTr = lastTr.clone();

                lastTr.find('.modalSelect2').select2({
                    dropdownParent: $('#add-schooltiming-modal')
                });

                clonedTr.find('.modalSelect2').select2({
                    dropdownParent: $('#add-schooltiming-modal')
                });

                $('#add-more-programAndDayWise-table tbody').append(clonedTr)
            }
            else if($('.programDaySessionWise[data-dis]').attr('data-dis') == 'true'){
                let lastTr = $('#add-more-programDaySessionWise-table tbody tr:last-child')
                lastTr.find('.modalSelect2').select2('destroy');
                let programName = lastTr.find(`select[name="programName"]`).val();
                let acadSession = lastTr.find(`select[name="acadSession"]`).val();
                let day = lastTr.find(`select[name="day"]`).val();
                let startTime = lastTr.find(`select[name="startTime"]`).val();
                let endTime = lastTr.find(`select[name="endTime"]`).val();
                let schoolTimingType = lastTr.find(`select[name="schoolTimingType"]`).val();
                let clonedTr = lastTr.clone();

                lastTr.find('.modalSelect2').select2({
                    dropdownParent: $('#add-schooltiming-modal')
                });

                clonedTr.find('.modalSelect2').select2({
                    dropdownParent: $('#add-schooltiming-modal')
                });

                $('#add-more-programDaySessionWise-table tbody').append(clonedTr)
            }
        })

        $('#add-more-sameForAllDay-table').on('click', '.remove-schoolTiming',
            function () {
                let trLength = $('#add-more-sameForAllDay-table tbody tr').length;

                if (trLength > 1) {
                    $(this).closest('tr').remove()
                } else {
                    alert('Cannot delete this.')
                }
            })

        $('.schoolTimingOptionsContainer').on('click', '.remove-schoolTimingDayWise',
            function () {
                let trLength = $('#add-more-DayWise-table tbody tr').length;
                console.log(trLength)
                if (trLength > 1) {
                    $(this).closest('tr').remove()
                } else {
                    alert('Cannot delete this.')
                }
            })
    
        $('.schoolTimingOptionsContainer').on('click', '.remove-schoolTimingProgramWise',
            function () {
                let trLength = $('#add-more-programAndDayWise-table tbody tr').length;

                if (trLength > 1) {
                    $(this).closest('tr').remove()
                } else {
                    alert('Cannot delete this.')
                }
            })

        $('.schoolTimingOptionsContainer').on('click', '.remove-schoolTimingProgramSessionWise',
            function () {
                let trLength = $('#add-more-programDaySessionWise-table tbody tr').length;

                if (trLength > 1) {
                    $(this).closest('tr').remove()
                } else {
                    alert('Cannot delete this.')
                }
            })
    
        //Api for onchange program event
        $('.schoolTimingOptionsContainer').on('change', '.programNameForSession', function() { 
            let program_lid = $(this).val();
                console.log('program_lid', program_lid)
            let apiObj = {
                type:"POST",
                url:"/admin/courseworkload/preference/acadSessionList",
                data: {
                    program_lid: program_lid
                },
                dataType:"JSON"
            }

            ajaxApi(apiObj).then(result => {
                let acadList = `<select class="form-select sessionForProgram" name="acadSession" id="">`
                    if(result.data.acadSessionList.length > 0){
                        for(let data of result.data.acadSessionList){
                        acadList += `<option value="${data.acad_session_lid}"> ${data.acad_session} </option>` 
                        }
                    }
                    else{
                        acadList += `<option value=""> NO SESSION FOUND <option>`
                    }
                    acadList += `</select>`
                    $(this).parent().next().html(acadList);

                console.log('list:::>>>', $(this).parent().next());
            }).catch(error => console.log('error',error));
        })

        $(".AddRoomPreference").on('click',function(){
            let school_timings_name = $("#school_timings_name").val()
            let program_lid = $("#program_name").val()// Get Value of Dropdown
            let program_text =  $("#program_name option:selected").text(); // Get Text of Dropdown
            let acad_session_lid = $("#acad_session").val();
            let acad_session_text =  $("#acad_session option:selected").text();
            let day_name_lid = $("#day_name").val();
            let day_text =  $( "#day_name option:selected").text();
            let slot_start_lid = $("#slot_start_lid").val();
            let slot_start_text =  $( "#slot_start_lid option:selected").text();
            let slot_end_lid = $("#slot_end_lid").val();
            let slot_end_text =  $( "#slot_end_lid option:selected").text();
            $("#add-more-schoolTiming-table tbody").append(`
            <tr>
                                    <td>
                                    #
                                    </td>
                                    <td data-school-timings-name="${school_timings_name}">
                                        ${school_timings_name}
                                    </td>
                                    <td data-program-lid="${program_lid}">
                                        ${program_text}
                                    </td>
                                    <td data-acad-session-lid="${acad_session_lid}">
                                        ${acad_session_text}
                                    </td>
                                    <td data-day-name-lid="${day_name_lid}">
                                        ${day_text}
                                    </td>
                                    <td data-slot-start-lid="${slot_start_lid}">
                                        ${slot_start_text}
                                    </td>
                                    <td data-start-end-lid="${slot_end_lid}">
                                        ${slot_end_text}
                                    </td>
                                    <td>
                                        <i class="far fa-edit d-none"></i>
                                        <i class="fas fa-trash-alt text-danger cursor-pointer remove-schoolTimingRow"></i>
                                    </td>
                                </tr>
                    `)

        })


        //Remove Element
        $("#add-more-schoolTiming-table").on('click', '.remove-schoolTimingRow', function () {
            $($(this)).fadeOut(300, function () {
               $($(this).parent().parent()).remove()
            })
        })



            //CREATING JSON FOR SCHOOL TIMING 
            $(".createSchoolTiming").on('click',function(){
            let schoolTimeList = $('#add-more-schoolTiming-table tbody tr');
            let schoolTimeCount = schoolTimeList.length;
            let schoolTimeJson = [];
            schoolTimeList.each(function (index, elem) {
                let school_timings_name = $(elem).find('td[data-school-timings-name]').attr("data-school-timings-name")
                let program_lid = $(elem).find('td[data-program-lid]').attr("data-program-lid")
                let acad_session_lid = $(elem).find('td[data-acad-session-lid]').attr("data-acad-session-lid")
                let day_name_lid = $(elem).find('td[data-day-name-lid]').attr("data-day-name-lid")
                let slot_start_lid = $(elem).find('td[data-slot-start-lid]').attr("data-slot-start-lid")
                let slot_end_lid = $(elem).find('td[data-start-end-lid]').attr("data-start-end-lid")


                if (index < schoolTimeCount - 1) {
                    let obj = {
                        school_timings_name: school_timings_name,
                        program_lid: program_lid,
                        acad_session_lid:acad_session_lid,
                        day_name_lid: day_name_lid,
                        slot_start_lid: slot_start_lid,
                        slot_end_lid: slot_end_lid
                    }
                    schoolTimeJson.push(obj)
                } else {
                    let obj = {
                        school_timings_name: school_timings_name,
                        program_lid: program_lid,
                        acad_session_lid:acad_session_lid,
                        day_name_lid: day_name_lid,
                        slot_start_lid: slot_start_lid,
                        slot_end_lid: slot_end_lid
                    }
                    schoolTimeJson.push(obj)
                }
            })
            console.log('schoolTimeJson:::::::::::::::::::::>>>>',schoolTimeJson)

            let apiObj = {
                type:"POST",
                url:"/admin/schooltime",
                data: {
                    inputJSON: schoolTimeJson
                },
                dataType:"JSON"
            }
            
            ajaxApi(apiObj).then(result => {
                console.log('result ::::::::::::::::::::::', result)
                showSuccess(result)
            }).catch(error => {
                console.log('Error ::::::::::::::::::::::', error)
                showError(error.responseJSON)
            })
        })


            //On change function
            $('#program_name').on('change', function() { 
            let program_lid = $("#program_name").val();
                
            let apiObj = {
                type:"POST",
                url:"/admin/courseworkload/preference/acadSessionList",
                data: {
                    program_lid: program_lid
                },
                dataType:"JSON"
            }

            ajaxApi(apiObj).then(result => {
                let acadList = ``
                    if(result.data.acadSessionList.length > 0){
                        for(let data of result.data.acadSessionList){
                        acadList += `<option value="${data.acad_session_lid}"> ${data.acad_session} </option>` 
                        }
                    }
                    else{
                        acadList += `<option value=""> NO SESSION FOUND <option>`
                    }
                    $("#acad_session").html(acadList);
                dayList =  `` 
                    if(result.data.dayList.length > 0){
                        for(let data of result.data.dayList){
                            dayList += `<option value="${data.day_lid}"> ${data.day_name} </option>` 
                        }
                    }
                    else{
                        dayList += `<option value=""> NO DAY FOUND <option>`
                    }
                    $("#day_name").html(dayList);
                console.log('list:::>>>', $(this).next());
            }).catch(error => console.log('error',error));
        })

 


        // SEARCH PROGRAM TYPE
        $('#searchkeyword').on('input', delay(function (e) {
            let keyword = $("#searchkeyword").val()
            $.ajax({
                type: "GET",
                url: "/admin/divisions/search",
                data: {
                    keyword: keyword
                },
                success: function (data) {
                    console.log(data)
                    AjaxtTable(data.data)
                }
            })
        }, 500));



        function showError(errors) {
            let errorHtml = `<div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <h3>Error</h3>
                    <p>${errors.description}</p>
                    <ul>`
            if (errors.data.length > 0) {
                $(errors.data).each(function (key, value) {
                    $.each(this, function (key, value) {
                        errorHtml +=
                            `<li>${key} ${value}</li>`
                    });
                });
            }
            errorHtml +=
                `</ul> <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button> </div>`
                $("#errorHtml").html(errorHtml)
        }


        function showSuccess(errors) {
            let errorHtml = `<div class="alert alert-success alert-dismissible fade show" role="alert">
                    <h3>Success</h3>
                    <p>${errors.description}</p>
                    <ul>`
            if (errors.data.length > 0) {
                $(errors.data).each(function (key, value) {
                    $.each(this, function (key, value) {
                        errorHtml +=
                            `<li>${key} ${value}</li>`
                    });
                });
            }
            errorHtml +=
                `</ul> <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button> </div>`
            $("#errorHtml").html(errorHtml)
        }


    })
</script>
<%- include("../partials/footerEnd") %>