<%- include("../partials/head") %>
<%- include("../partials/leftSidebar") %>
<%- include("../partials/header") %>
<!--Notificaion icon-->
<!--HEADER END-->

<div class="main-content">
    <!--DASHBOARD CONTENT START-->
    <div class="row my-3 gx-3 gy-3 row1-data">
        
        <div class="col-xl-2 col-lg-4 col-md-6">
            <a href="/admin/faculties/date-time">
            <div class="card row1-data-card text-center">
                    <div class="card-body d-flex flex-column align-items-center justify-content-center">
                        <img src="/icon_imgs/room-type.PNG" alt="">
                        <p class="fw-normal pt-1">FACULTY DATE TIME</p>
                    </div>
            </div>
            </a>
        </div>

        <div class="col-xl-2 col-lg-4 col-md-6">
            <a href="/admin/faculties/works">
            <div class="card row1-data-card text-center">
                    <div class="card-body d-flex flex-column align-items-center justify-content-center">
                        <img src="/icon_imgs/room-type.PNG" alt="">
                        <p class="fw-normal pt-1">FACULTY WORKS</p>
                    </div>
            </div>
            </a>
        </div>

        <div class="col-xl-2 col-lg-4 col-md-6">
            <div class="card row1-data-card text-center">
                <a href="/admin/faculties/workpreferences">
                    <div class="card-body d-flex flex-column align-items-center justify-content-center">
                        <img src="/icon_imgs/room-type.PNG" alt="">
                        <p class="fw-normal pt-1">WORK TIME PREFERENCE</p>
                    </div>
            </div>
            </a>
        </div>

        <div class="col-xl-2 col-lg-4 col-md-6">
            <a href="/admin/faculties/batch">
                <div class="card row1-data-card text-center">
                    <div class="card-body d-flex flex-column align-items-center justify-content-center">
                        <img src="/icon_imgs/room-type.PNG" alt="">
                        <p class="fw-normal pt-1">FACULTY BATCH</p>
                    </div>
                </div>
            </a>
        </div>

    </div>


    <div class="card table-card">
        <div class="card-header table-card-header text-uppercase d-flex align-items-center justify-content-between">
            <div>
                <h5>Add Faculty</h5>
            </div>
            <div>
                <button class="btn add-btn ms-auto" data-bs-toggle="modal" data-bs-target="#open-faculty-modal"><i
                        class="fas fa-plus"></i></button>
                <button class="btn ms-auto delete-campuses add-btn "><i class="fas fa-trash"></i></button>
                <button class="btn ms-auto bulk-delete-campuses add-btn "><img src="/icons/delete.png" alt=""
                        width="26px" height="32px"></button>
            </div>
        </div>

        <div class="card-body table-responsive">
            <div class="table-searchbar-container"><button type="button"><i class="fas fa-search"></i> </button><input
                    type="search" id="searchkeyword" placeholder="Enter keywords" class="table-searchbar"></div>
            <table class="table custom_row" id="facultyTable">
                <thead>
                    <th>Sr.No.</th>
                    <th>Faculty Id</th>
                    <th>Faculty Name</th>
                    <th>Action</th>
                    <th>All <input type="checkbox" class="selectAll-faculties"></th>
                </thead>
                <tbody>
                    <% let count = 1%>
                    <%for(let faculty of facultyList){%>
                    <tr>
                        <td><%- count++ %></td>
                        <td data-faculty-id="<%- faculty.id %>"><%- faculty.faculty_id %></td>
                        <td><%- faculty.faculty_name %></td>
                        <td><%- faculty.id %></td>
                        <td><%- faculty.id %></td>
                    </tr>
                    <%}%>
                </tbody>
            </table>
            <div class="d-flex justify-content-between">
                <div>
                    <p>Total entries:<%- pageCount %>
                    </p>
                </div>
                <div>
                    <p id="pagination"></p>
                </div>
            </div>
        </div>
    </div>
</div>


<!-- IMPORT FACULTY MODAL -->
<!-- Modal -->
<div class="modal fade" id="open-faculty-modal" tabindex="-1" aria-labelledby="open-faculty-modal" aria-hidden="true">
    <div class="modal-dialog modal-fullscreen modal-lg">
        <div class="modal-content modal-inputform">
            <div class="modal-header modal-inputform-header">
                <h3 class="modal-title" id="open-faculty-modal-title">Import Faculty</h3>
                <button type="button" class="btn fs-4 text-white" data-bs-dismiss="modal" aria-label="Close"><i
                        class="fas fa-times text-dark"></i></button>
            </div>
           
            <div class="modal-body py-0">
                
            <div class="row">
                <div class="col-md-6 select-items">
                    <h4 class="text-center py-1">Select Faculties</h4>
                    <input type="text" name="" id="searchItem" placeholder="Search Faculty" class="form-control">
                    <ul class="list-group custom-select-list" id="selectList">
                       
                        <% for (let faculty of faculties) { %>
                    <li class="list-group-item my-1 border-top" data-faculty-name="<%- faculty.faculty_name %>"
                        data-faculty-id="<%- faculty.faculty_id%>" data-clickable="true"><%- faculty.faculty_name %> (<%- faculty.faculty_id%>)
                        %></li>
                    <% } %>
                    </ul>
        </div>
        <div class="col-md-6 selected-items">
            <h4 class="text-center py-1">Selected Faculties</h4>
            <input type="text" name="" id="searchSelectedItem" placeholder="Search Selected Faculty"
                class="form-control">
            <ul class="list-group custom-select-list" id="selectedList">

            </ul>

        </div>

    </div>
</div>
<div class="px-3" id="errorHtml"></div>
<div class="modal-footer">
    <button type="submit" class="btn  createFaculty modal-inputform-btn">Create Faculty</button>
</div>
</div>
</div>
</div>

<%- include("../partials/notification") %>
<%- include("../partials/footer") %>
<script src="https://cdn.rawgit.com/botmonster/jquery-bootpag/master/lib/jquery.bootpag.min.js"></script>
<script src="/js/leftsidebartoggle.js"></script>

<script>
    $(document).ready(function () {
        //DATALIST



        $("#selectList").on('click', '.list-group-item', function () {

            //remove current element when added to right sidebar
            if($(this).attr('data-clickable') == 'true'){
                $("#selectedList").append(`<li class="list-group-item d-flex justify-content-between align-items-center my-1 border-top"  data-faculty-name="${$(this).attr  ("data-faculty-name")}" data-faculty-id="${$(this).attr("data-faculty-id")}" data-clickable="true">${$(this).text()} <span class="badge bg-danger rounded-pill clicktoremove ms-auto me-2">Remove</span>
                    <div class="form-check form-switch ms-3" data-faculty-id="${$(this).attr("data-faculty-id")}">
                        <input class="form-check-input faculty-switch" type="checkbox" id="flexSwitchCheckDefault">
                        <label class="form-check-label label-core" style="width:45px" for="flexSwitchCheckDefault" data-core="">Core</label>
                    </div>
                    </li> `)
            }
      
            $(this).attr('data-clickable','false');

            $($(this)).fadeOut(300, function () {
                $($(this)).remove()
            })
        
        })

        $("#selectedList").on('click','.faculty-switch', function() {
            
            if($(this).parent().find('label').text() == 'Core'){
                $(this).parent().find('label').text("Visiting");
            }
            else{
                $(this).parent().find('label').text("Core");
            }

        })

        //Remove Element

        $("#selectedList").on('click', '.clicktoremove', function () {

            if($(this).parent().attr('data-clickable') == 'true'){
                    $("#selectList").append(`<li class="list-group-item my-1 border-top" data-faculty-name="${$(this).parent().attr("data-faculty-name")}" data-faculty-id="${$(this).parent().attr("data-faculty-id")}" data-clickable="true"> ${$(this).parent().attr("data-faculty-name")} (${$(this).parent().attr("data-faculty-id")})
			        </li>`)
            }

            $(this).parent().attr('data-clickable','false')

            $($(this)).fadeOut(300, function () {
                $($(this).parent()).remove()
            })
        })
       
        //search 
        let faculties = $('#selectList li')
    

        $('#searchItem').on('input', function () {

            var text = $('#searchItem').val().toLowerCase();

            for (let faculty of faculties) {
                if (faculty.textContent.toLowerCase().indexOf(text) > -1) {
                    $(faculty).removeClass("d-none");
                } else {
                    $(faculty).addClass("d-none");
                }
            };
        })

        //search selected faculty

        $('#searchSelectedItem').on('input', function () {
            let selectedFaculties = $('#selectedList li')
            var text = $('#searchSelectedItem').val().toLowerCase();
            console.log('text:::::::::', selectedFaculties)
            for (let faculty of selectedFaculties) {
                console.log('faculty:::::::::', faculty)
                if (faculty.textContent.toLowerCase().indexOf(text) > -1) {

                    $(faculty).removeClass("d-none");
                } else {
                    $(faculty).addClass("d-none");
                }
            };
        })

        //Create New Faculty Entry
        $(".createFaculty").on('click', function () {
            let facultiesList = $('#selectedList li');
            let facultyCount = facultiesList.length;
            let facultyJson = [];
            facultiesList.each(function (index, elem) {
                let faculty_id = $(elem).attr("data-faculty-id")
                let faculty_name = $(elem).attr("data-faculty-name")
                
                console.log('facultyName',faculty_name)

                if (index < facultyCount - 1) {
                    let obj = {
                        id: faculty_id,
                        
                    }
                    facultyJson.push(obj)
                } else {
                    let obj = {
                        id: faculty_id,
                      
                    }
                    facultyJson.push(obj)
                }
            })

            console.log('facultyJson::::::::::::>>', facultyJson)
            let ApiObj = {
                type: 'POST',
                url: '/admin/faculties',
                data: {
                    inputJSON: JSON.stringify(facultyJson)
                },
                dataType: 'JSON'
            }

            ajaxApi(ApiObj).then(result => {
                console.log('sucess:::::::::',result)
                notify()
            }).catch(error => {
                showError(error.responseJSON)
            })
        })

    
    //PAGINATION
    let pageCount = `<%- pageCount %>`
    
    let pageNos = Math.ceil(Number(pageCount) / 10)

    console.log("Page Numbers", pageNos)
    $('#pagination').bootpag({
        total: pageNos,
        page: 1,
        maxVisible: 10,
        leaps: true,
        firstLastUse: true,
        first: '←',
        last: '→',
        wrapClass: 'pagination',
        activeClass: 'active', 
        disabledClass: 'disabled',
        nextClass: 'next',
        prevClass: 'prev',
        lastClass: 'last',
        firstClass: 'first'
    }).on("page", function (event, num) {
        console.log('pagination here!!')
        $.ajax({
            type: "POST",
            url: "/admin/faculties/pagination",
            data: {
                pageNo: num
            },
            success: function (data) {
                showAjaxTable(data.data) 
            }
        })
    });



        //search feature delay function is writter in script.js file which is located in public/js/script.js
        $('#searchkeyword').on('input', delay(function (e) {
            let keyword = $("#searchkeyword").val()
            $.ajax({
                type: "GET",
                url: "/management/faculties/search",
                data: {
                    keyword: keyword
                },
                success: function (data) {
                    showAjaxTable(data.data) // Facilty List
                }
            })
        }, 500));



        function showError(errors) {
            console.log('Error:::::::::',errors)
            let errorHtml = `<div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <h3>Error!</h3>
                    <p>${errors.description}</p>
                    <ul>`
            if (errors.data.length > 0) {
                $(errors.data).each(function (key, value) {
                    $.each(this, function (key, value) {
                        errorHtml +=
                            `<li>${key} ${value}</li>`
                    });
                });
            }
            errorHtml +=
                `</ul> <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button> </div>`
                console.log(errorHtml)
            $("#errorHtml").html(errorHtml)
        }

        function showAjaxTable(facultiesList) {
            let AjaxTable = `<table class="table custom_row" id="holidayTable">
                <thead>
                    <th>Faculty Id</th>
                    <th>Faculty Name</th>
                    <th>Start Time</th>
                    <th>End Time</th>
                    <th>Campus</th>
                    <th>Organization</th>
                    <th>Action</th>
                    <th>All <input type="checkbox" class="selectAll-faculties"></th>
                </thead>
                    <tbody>`;

            if (facultiesList.length > 0) {
                for (building of facultiesList) {
                    AjaxTable +=
                        `<tr>
                            <td>${building.building_name}</td>
                            <td>${building.building_number}</td>
                            <td>${building.total_floors}</td>
                            <td>${building.owner}</td>
                            <td>${building.handled_by}</td>
                            <td>${building.start_time}</td>
                            <td>${building.end_time}</td>
                            <td>${building.campus_abbr}</td>
                            <td>
                                <i class="fas fa-edit text-dark cursor-pointer edit-building"
                                    data-building-id="${building.building_id}" ></i>
                            </td>
                            <td>
                            <input type="checkbox" data-building-id="${building.building_id}" 
                                class="delete-building">
                        </td>
                        </tr>`
                }
            } else {
                AjaxTable +=
                    ` <tr>
                                <td colspan="9">No Data Found</td>
                            </tr>`;
            }
            AjaxTable += `<tbody></table>`
            $("#buildingTable").html(AjaxTable);

        }

    })
</script>
<%- include("../partials/footerEnd") %>